{
  "name": "commoncrane",
  "private": true,
  "version": "0.1.0",
  "license": "MIT",
  "type": "module",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "typecheck": "tsc -b --noEmit packages/room",
    "test": "vitest run",
    "test:consumers": "node scripts/consumers.mjs",
    "release": "node scripts/release.mjs",
    "pin": "node scripts/pin.mjs",
    "build": "npm run build --workspace=@johnnypickles/room"
  },
  "devDependencies": {
    "typescript": "^5.9.0",
    "vitest": "^4.0.0"
  }
}
